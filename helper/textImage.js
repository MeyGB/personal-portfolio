export default function textImage(name) {
  if (!name) return "";

  const namePath = name.trim().split("");
  if (namePath.length === 1) {
    const result1 = namePath[0].charAt(0).toUpperCase;
  }
}
